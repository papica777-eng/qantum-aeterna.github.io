version: '3.8'

services:
  # Rust Backend - lwas_core API
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: aeterna-backend
    restart: unless-stopped
    ports:
      - "8890:8890"
    environment:
      - RUST_LOG=${RUST_LOG:-info}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - STRIPE_SUCCESS_URL=${STRIPE_SUCCESS_URL:-https://aeterna.website/success}
      - STRIPE_CANCEL_URL=${STRIPE_CANCEL_URL:-https://aeterna.website/cancel}
      - EXCHANGE_API_KEY=${EXCHANGE_API_KEY}
      - EXCHANGE_SECRET_KEY=${EXCHANGE_SECRET_KEY}
      - PORT=8890
    volumes:
      - ./AETERNA_ANIMA.soul:/AETERNA_ANIMA.soul:ro
      - ./SOVEREIGN.ledger:/app/SOVEREIGN.ledger
      - aeterna-data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8890/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - aeterna-network

  # TypeScript Middleware - OmniCore
  middleware:
    build:
      context: ./OmniCore
      dockerfile: ../Dockerfile.middleware
    container_name: aeterna-middleware
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "8765:8765"
    environment:
      - NODE_ENV=production
      - HTTP_PORT=3000
      - WS_PORT=8765
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - EXCHANGE_API_KEY=${EXCHANGE_API_KEY}
      - EXCHANGE_SECRET_KEY=${EXCHANGE_SECRET_KEY}
      - CORS_ORIGIN=${CORS_ORIGIN:-*}
      - POSTGRES_URL=postgresql://postgres:password@postgres:5432/aeterna
    depends_on:
      backend:
        condition: service_healthy
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/status", "||", "exit", "1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - aeterna-network

  # Database - PostgreSQL
  postgres:
    image: postgres:15-alpine
    container_name: aeterna-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=aeterna
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aeterna-network

  # Frontend - Nginx serving React build
  frontend:
    build:
      context: ./helios-ui
      dockerfile: ../Dockerfile.frontend
    container_name: aeterna-frontend
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - BACKEND_URL=${BACKEND_URL:-http://backend:8890}
      - WS_URL=${WS_URL:-ws://middleware:8765}
    depends_on:
      - backend
      - middleware
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    networks:
      - aeterna-network

  # Reverse Proxy - Nginx
  nginx:
    image: nginx:alpine
    container_name: aeterna-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./ssl:/etc/nginx/ssl:ro
      - nginx-logs:/var/log/nginx
    depends_on:
      - backend
      - middleware
      - frontend
    networks:
      - aeterna-network

volumes:
  aeterna-data:
    driver: local
  nginx-logs:
    driver: local
  pg_data:
    driver: local

networks:
  aeterna-network:
    driver: bridge
